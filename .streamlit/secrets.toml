# Streamlit Cloud Secrets Configuration
# Copia estos valores en: https://share.streamlit.io/
# → Settings → Secrets

# URL del backend (será asignada por Render)
BACKEND_URL = "http://localhost:8000"

# OpenAI API Key
OPENAI_API_KEY = "sk-proj-[tu-key]"
